using UnityEngine;
using System.Collections;

public class Stick: Ingredient {
    
    public Stick(Vector3 loc) : base(1, loc) { Debug.Log("Stick loaded"); }
    // Use this for initialization
    void Start () {
        
	}
	
	// Update is called once per frame
	void Update () {
        
    }
}
